---
description: 提交前需在 CHANGELOG.md 中记录本次更新内容
alwaysApply: true
---

# 提交前更新更改日志

在 **git commit 之前**，必须更新项目根目录的 `CHANGELOG.md`：

1. 打开 `CHANGELOG.md`，找到 **`[Unreleased]`** 区块。
2. 根据本次修改类型，在对应小节下添加一条或多条说明：
   - **新增 (Added)**：新卡牌、遗物、事件、机制等
   - **变更 (Changed)**：已有内容的数值、描述、逻辑调整
   - **修复 (Fixed)**：Bug 修复、文案纠错
   - **移除 (Removed)**：删除的功能或内容
3. 每条说明尽量简洁、可读，例如：
   - `- 新增稀有攻击牌「死亡收割」`
   - `- 调整王筱巫玩偶描述文案`
   - `- 修复商店折扣未生效的问题`

若本次提交仅含文档、配置或无关玩法的改动，可在对应小节写「无」或跳过该小节，但 **有功能/内容改动时务必更新 CHANGELOG**。
